apiVersion: v1
kind: Service
metadata:
  name: {{ include "midas-backend.fullname" . }}
  labels:
    {{- include "midas-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "midas-backend.selectorLabels" . | nindent 4 }}
