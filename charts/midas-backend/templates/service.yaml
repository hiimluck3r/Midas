apiVersion: v1
kind: Service
metadata:
  name: {{ include "midas-backend.fullname" . }}-db
  labels:
    {{- include "midas-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.services.db.port }}
      targetPort: db
      protocol: TCP
      name: db
  selector:
    {{- include "midas-backend.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "midas-backend.fullname" . }}-api
  labels:
    {{- include "midas-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.services.api.port }}
      targetPort: api
      protocol: TCP
      name: api
  selector:
    {{- include "midas-backend.selectorLabels" . | nindent 4 }}
