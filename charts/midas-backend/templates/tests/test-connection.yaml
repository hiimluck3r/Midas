apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midas-backend.fullname" . }}-test-connection"
  labels:
    {{- include "midas-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midas-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
