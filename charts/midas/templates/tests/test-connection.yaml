apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midas.fullname" . }}-test-connection"
  labels:
    {{- include "midas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
