apiVersion: v1
kind: Service
metadata:
  name: {{ include "midas.fullname" . }}
  labels:
    {{- include "midas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "midas.selectorLabels" . | nindent 4 }}
