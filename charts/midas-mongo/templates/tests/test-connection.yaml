apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midas-mongo.fullname" . }}-test-connection"
  labels:
    {{- include "midas-mongo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midas-mongo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
