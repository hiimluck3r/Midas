apiVersion: v1
kind: Service
metadata:
  name: {{ include "midas-mongo.fullname" . }}
  labels:
    {{- include "midas-mongo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "midas-mongo.selectorLabels" . | nindent 4 }}
