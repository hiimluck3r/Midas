apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "midasbackend.fullname" . }}-test-connection"
  labels:
    {{- include "midasbackend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "midasbackend.fullname" . }}:{{ .Values.services.api.port }}']
  restartPolicy: Never
